export enum RouteLink {
  home = '/',
  wallet = '/wallet',
  profile = '/profile',
  verification = '/verification',
  classicDice = '/classicDice',
}

export enum RouteName {
  home = 'home',
  wallet = 'wallet',
  profile = 'profile',
  verification = 'verification',
  classicDice = 'classicDice',
}
