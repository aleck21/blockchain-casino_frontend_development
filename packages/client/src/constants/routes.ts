export enum RouteLink {
  home = '/',
  wallet = '/wallet',
  profile = '/profile',
  verification = '/verification',
}

export enum RouteName {
  home = 'home',
  wallet = 'wallet',
  profile = 'profile',
  verification = 'verification',
}
